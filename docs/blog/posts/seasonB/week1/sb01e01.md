---
date:
  created: 2025-11-03
  draft: true
---

# **📅 SAISON B Semaine 1 Épisode 1 - Découverte de Node.js**

## **📚 Notions du jour**

+ **NodeJS**, c’est quoi ?
+ Les modules natifs, ces objets bien pratiques qu’on récupère grâce à import
+ le module **[Readline](https://nodejs.org/api/readline.html)**

<!-- more -->

---

### **🌐 Node.js, C’est Quoi ?**

Aujourd’hui, je plonge dans un **nouveau monde** : le **backend** avec **Node.js** ! Jusqu’à présent, j’utilisais JavaScript pour créer des interfaces dynamiques dans le navigateur (grâce à Svelte). Mais Node.js, c’est **JavaScript côté serveur** ! 🚀

**En résumé :**

- **Node.js** est un **environnement d’exécution** pour JavaScript, qui permet de lancer du code **hors du navigateur** (sur un serveur, par exemple).
- Il utilise le **moteur V8** (le même que Chrome) pour exécuter du JavaScript.
- Il est **asynchrone et événementiel**, ce qui le rend idéal pour les applications qui nécessitent des **requêtes réseau, des fichiers, ou des bases de données**.

**Pourquoi c’est révolutionnaire ?**

- **Un seul langage** (JavaScript) pour le frontend **et** le backend.
- **Une communauté énorme** et des milliers de modules disponibles via **npm** (Node Package Manager).
- **Performant** grâce à son modèle non-bloquant (parfait pour les applications en temps réel).

---

### **📦 Les Modules Natifs : Des Outils Prêts à l’Emploi**

Node.js vient avec des **modules natifs** (ou *built-in modules*), qui permettent d’effectuer des tâches courantes sans installer de dépendances externes. On les importe avec la syntaxe `import <module> from "node:<module>"`.

Voici une **sélection des modules les plus utiles**, avec des exemples concrets pour chacun.

| Module          | Utilité                                                                 |
|-----------------|------------------------------------------------------------------------|
| `fs`            | Interagir avec le système de fichiers (lire/écrire des fichiers).      |
| `path`          | Manipuler les chemins de fichiers (indépendamment du système d’exploitation). |
| `http`          | Créer un serveur web ou faire des requêtes HTTP.                      |
| `events`        | Gérer des événements personnalisés.                                   |
| `readline`      | Lire des entrées utilisateur depuis la console (ligne par ligne).    |

---

#### **📁 Le Module `fs` : Manipuler le Système de Fichiers**

##### **Exemples d’Utilisation :**

1. **Lire un fichier :**

  ```javascript
  // Import the 'fs' module to work with files
  import fs from 'node:fs';

  // Read a file asynchronously
  fs.readFile('example.txt', 'utf8', (err, data) => {
    if (err) {
      console.error("Error reading file:", err);
      return;
    }
    console.log("File content:", data);
  });
  ```

2. **Écrire dans un fichier :**

  ```javascript
  import fs from 'node:fs';

  try {
    fs.writeFileSync('file.txt', 'Hello, Node.js !');
    console.log("File written successfully!");
  } catch (err) {
    console.error("File write error :", err);
  }
  ```

3. **Lister les fichiers d’un dossier :**

  ```javascript
  import fs from 'node:fs';

  fs.readdir('.', (err, files) => {
    if (err) {
      console.error("Error :", err);
      return;
    }
    console.log("Files in the directory :", files);
  });
  ```

**Cas d’usage :**

+ Sauvegarder des données utilisateur.
+ Lire des fichiers de configuration.
+ Traiter des logs ou des exports.

---

#### **🗺️ Le Module `path` : Gérer les Chemins de Fichiers**

Le module `path` permet de **manipuler les chemins de fichiers** de manière **cross-platform** (compatible Windows, Linux, macOS). Plus besoin de se soucier des `/` ou `\` !

##### **Exemples d’Utilisation :**

```javascript
// Import the 'path' module to handle file paths
import path from 'node:path';

// Join path segments to create a cross-platform path
const filePath = path.join('folder', 'subfolder', 'file.txt');
console.log("Constructed path:", filePath);
// Output (on Unix): "folder/subfolder/file.txt"
// Output (on Windows): "folder\subfolder\file.txt"
```
1. **Construire un chemin absolu :**

  ```javascript
  // Import the 'path' module to handle file paths
  import path from 'node:path';

  // Join path segments to create a cross-platform path
  const fullPath = path.join(__dirname, 'directory', 'file.txt');
  console.log("Full path:", fullPath);
  // Output (on Unix) : "/absolute/path/directory/file.txt"
  // Output (on Windows) : "C:\absolute\path\directory\file.txt"
  ```

2. **Extraire le nom de fichier d’un chemin :**

  ```javascript
  const fileName = path.basename('/path/to/file.txt');
  console.log("File name:", fileName); // "file.txt"
  ```

3. **Récupérer l’extension d’un fichier :**

  ```javascript
  const ext = path.extname('file.txt');
  console.log("Extension:", ext); // ".txt"
  ```

**Cas d’usage :**

+ Construire des chemins dynamiques dans une application.
+ Valider ou parser des chemins utilisateur.
+ Évite les problèmes de compatibilité entre Windows, Linux et macOS.

---

#### **🌍 Le Module `os` : Interagir avec le Système d’Exploitation**

Le module `os` fournit des **informations sur le système d’exploitation** et l’environnement d’exécution. Idéal pour adapter le comportement de votre application en fonction de la plateforme.

##### **Exemples d’Utilisation :**

1. **Récupérer le système d’exploitation :**

   ```javascript
   import os from 'node:os';

   console.log("OS:", os.platform());
   // "linux", "win32", "darwin" (macOS), etc.
   ```

2. **Connaître l’architecture du processeur :**

   ```javascript
   console.log("Architecture :", os.arch());
   // "x64", "arm64", etc.
   ```

3. **Obtenir des informations sur la mémoire :**

   ```javascript
   console.log("Memory:", os.totalmem() / 1024 / 1024, "Mb");
   console.log("Free memory :", os.freemem() / 1024 / 1024, "Mb");
   ```

4. **Récupérer les interfaces réseau :**

   ```javascript
   console.log("Networks:", os.networkInterfaces());
   // Print IP addresses, MAC addresses, etc.
   ```

5. **Connaître le répertoire personnel de l’utilisateur :**

   ```javascript
   console.log("User folder:", os.homedir());
   // "/home/user" (Linux/macOS) or "C:\Users\user" (Windows)
   ```

**Cas d’usage :**

+ Adapter le comportement de l’application en fonction de l’OS (ex: chemins de fichiers, commandes système).
+ Optimiser les performances en fonction des ressources disponibles (CPU, mémoire).
+ Personnaliser l’expérience utilisateur (ex: langue, répertoire de sauvegarde).

---

#### **🔠 Le Module `readline` : Lire des Entrées Utilisateur en Console**

Le module `readline` permet de **lire des entrées utilisateur depuis le terminal**, ligne par ligne. C’est parfait pour créer des **interfaces en ligne de commande (CLI)** ou des scripts interactifs.

---

#### **📖 Exemple : Un Script Interactif avec `readline`**

```javascript
// Import the 'readline' module to read user input
import * as readline from 'node:readline';

// Create an interface to read from stdin and write to stdout
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

// Ask the user a question
rl.question("What's your name? ", (name) => {
  console.log(`Hello, ${name}!`);

  // Close the interface
  rl.close();
});
```

**Explications :**

1. **`readline.createInterface`** : Crée une interface pour lire depuis `stdin` (l’entrée standard) et écrire vers `stdout` (la sortie standard).
2. **`rl.question`** : Pose une question à l’utilisateur et attend sa réponse.

**Cas d’usage :**

+ Créer un **menu interactif** en ligne de commande.
+ Automatiser des tâches avec des **questions/réponses** (ex: configuration d’un projet).

---

### **💡 Pourquoi Utiliser les Modules Natifs ?**

1. **Pas de dépendances externes** : Moins de risques de vulnérabilités ou de conflits de versions.
2. **Performances optimisées** : Ces modules sont **compilés avec Node.js**, donc très rapides.
3. **Portabilité** : Fonctionnent sur tous les systèmes (Windows, Linux, macOS).
4. **Documentation officielle** : Bien documentés et maintenus par l’équipe Node.js.

---

### **💡 Ce Que J’ai Appris Aujourd’hui**

1. **Node.js étend JavaScript au backend** : Plus besoin d’un navigateur pour exécuter du JS !
2. **Les modules natifs** sont des outils puissants pour des tâches courantes (fichiers, chemins, entrées/sorties).
3. **`readline`** permet de créer des **scripts interactifs** en ligne de commande, idéal pour des outils CLI ou des prototypes rapides.

---

## **⌨️ Challenge du jour : "Hit MO'chine" – Un Outil en Ligne de Commande avec Node.js !**

---

### **🎤 Présentation du Challenge**

Vous vous souvenez de cette émission des années 90 qui présentait le classement du Hit Parade ?  
Aujourd’hui, on va créer un **outil en ligne de commande (CLI)** pour Charlu et Lili, deux présentateurs fictifs qui veulent afficher les **10 premières entrées du hit-parade**. L’objectif est de réaliser un script Node.js qui interagit avec l’utilisateur via la console, en utilisant le **système événementiel** de Node.js et le module `readline`.

**Fonctionnalités demandées :**

1. **Afficher un message d’accueil** : `"Que souhaitez-vous faire ?"`.
2. **Répondre à 3 commandes** :

  + `chante` : Affiche un message personnalisé.
  + `classement` : Affiche les 10 premières entrées du Top 50 (à partir d’un tableau fourni).
  + `quitter` : Ferme le programme.

3. **Bonus** : Ajouter une commande `position n` pour afficher l’entrée à la position `n` du classement (ex: `position 5` affiche le *Hit* en position 5 du classement).

---

### **🎯 Objectif du Challenge**

Créer un **script interactif en Node.js** qui :

+ Utilise le module `readline` pour **lire les entrées utilisateur**.
+ Gère des **événements** pour répondre aux commandes.
+ Affiche des **données dynamiques** (le classement du Top 50).

**Les données nous sont fournies sous forme de tableau JavaScript nommé `hitParade`**

---

### **📌 Étapes Réalisées**

#### **1️⃣ Initialisation du Projet et Import des Modules**

**Objectif :** Créer un fichier `index.mjs` et importer les modules nécessaires (`readline`).

**Mon code :**
*(Insérer ici ton bloc de code pour l’initialisation du projet et l’import des modules. Par exemple :)*

```javascript
// Import the 'readline' module to handle user input
import readline from 'readline';

// Top 50 data
const top50 = [
  { position: 1, title: "Coldplay - Viva La Vida", artist: "Coldplay" },
  { position: 2, title: "Daft Punk - Get Lucky", artist: "Daft Punk" },
  // ... (le reste du tableau)
];

// Create the readline interface
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});
```

---

#### **2️⃣ Afficher le Message d’Accueil et Gérer les Commandes**

**Objectif :** Demander à l’utilisateur `"Que souhaitez-vous faire ?"` et écouter ses réponses pour déclencher les actions correspondantes.

**Mon code :**
*(Insérer ici ton bloc de code pour le message d’accueil et la gestion des commandes. Par exemple :)*

```javascript
// Display the welcome message and prompt the user
function askQuestion() {
  rl.question("Que souhaitez-vous ? ", (answer) => {
    const command = answer.trim().toLowerCase();

    // Handle the 'chante' command
    if (command === "chante") {
      console.log("Je m'appelle Charlu, je m'appelle Lili, vous êtes chez O'clock !");
    }
    // Handle the 'classement' command
    else if (command === "classement") {
      console.log("\n--- Top 10 du Hit-Parade ---");
      top50.forEach((entry) => {
        console.log(`${entry.position}. ${entry.title} (${entry.artist})`);
      });
    }
    // Handle the 'quitter' command
    else if (command === "quitter") {
      console.log("Au revoir !");
      rl.close();
      return;
    }
    // Handle the 'position n' command (bonus)
    else if (command.startsWith("position ")) {
      const position = parseInt(command.split(" ")[1]);
      if (position >= 1 && position <= 10) {
        const entry = top50.find((e) => e.position === position);
        console.log(`\nPosition ${position} : ${entry.title} (${entry.artist})`);
      } else {
        console.log("Position invalide. Veuillez entrer un nombre entre 1 et 10.");
      }
    }
    // Handle unknown commands
    else {
      console.log("Commande inconnue. Essayez 'chante', 'classement', 'position n' ou 'quitter'.");
    }

    // Ask the question again
    askQuestion();
  });
}

// Start the program
askQuestion();
```

---

#### **3️⃣ Gérer la Fermeture du Programme**

**Objectif :** Fermer proprement l’interface `readline` quand l’utilisateur tape `quitter`.

**Mon code :**
*(Insérer ici ton bloc de code pour la fermeture du programme. Par exemple :)*

```javascript
// Handle the 'close' event to exit the program
rl.on('close', () => {
  console.log("\nÀ bientôt chez O'clock !");
  process.exit(0);
});
```

#### **4️⃣ Bonus : afficher l’entrée à la position `n` du classement**

---

**Bonus : Commande `position n`** :

   - Utilisation de `startsWith()` pour détecter la commande.
   - Extraction du numéro avec `split()` et `parseInt()`.
   - Recherche de l’entrée correspondante dans le tableau `top50`.

---
### **🖥️ Rendu Final du Programme**

*(Insérer ici une capture d’écran ou un exemple de sortie console montrant l’interaction avec le programme. Par exemple :)*


---

### **💡 Ce Que J’ai Appris avec ce Challenge**

1. **Utiliser `readline`** pour créer des **interfaces en ligne de commande interactives**.
2. **Gérer les entrées utilisateur** de manière asynchrone avec des callbacks.
3. **Découper et analyser des commandes** (ex: `position 5`) pour extraire des informations.
4. **Fermer proprement un programme** avec `rl.close()`.

---
